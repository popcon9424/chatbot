# f = open('names.txt','w',encoding="utf-8")
# f.write('최현호')
# f.close()

# f = open('names.txt','r',encoding="utf-8")
# print(f.read())
# f.close()

# f = open('names.txt','a',encoding="utf-8")
# f.write("최현호\n")
# f.close()


## 영구적(손실없이)으로 데이터를 저장할 때

# .txt(텍스트) 파일을 연다
# 1. 읽기(Read)
# 2. 쓰기(Write)
# 3. 수정(Append)
# 파일을 닫는다

# .json 파일을 연다 (import json) => dict
# 1. 읽기(Read)
# 2. 쓰기(Write)
# 3. 수정(Append)
# 파일을 닫는다

# .csv 파일을 연다 (import csv) => list^2 (2d-list)
# 1. 읽기(Read)
# 2. 쓰기(Write)
# 3. 수정(Append)
# 파일을 닫는다

# with 키워드 활용 -> 코드를 간결하게

# DB를 연다(connect) -> CRUD
# 1. 읽기(CREATE)
# 2. 쓰기(READ / RETRIEVE)
# 3. 수정(UPDATE)
# 4. 삭제(DELETE / DESTROY)
# DB를 닫는다(disconncet)

# dictionary(key-value) + 강력한 메소드 추가 = object

# << ORM >> : 사용 빈도 매우매우매우매우매우매우매우매우 높음